---
- name: Setup db container
  docker_container:
    name: postgres
    image: postgres
    state: started
    volumes_from: data
    ports:
      - "5432:5432"
    env:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: "{{ pg_admin_pw }}"
  notify: "wait_for_postgres"